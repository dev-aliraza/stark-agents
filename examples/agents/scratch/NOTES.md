# Not an agent

This directory has no `AGENT.md` at its root, so discovery skips it silently. That is how
you keep shared notes, fixtures, or scratch files next to your agents without Stark trying
to load them.
